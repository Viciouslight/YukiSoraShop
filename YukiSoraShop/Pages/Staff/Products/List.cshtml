@page
@model YukiSoraShop.Pages.Staff.Products.StaffProductsModel
@{
    ViewData["Title"] = "Quáº£n lÃ½ sáº£n pháº©m";
}

<div class="row">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>
                <i class="fas fa-box text-primary"></i> Quáº£n lÃ½ sáº£n pháº©m
            </h1>
            <a href="/Staff/Products/Create" class="btn btn-primary">
                <i class="fas fa-plus"></i> ThÃªm sáº£n pháº©m má»›i
            </a>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="fas fa-list"></i> Danh sÃ¡ch sáº£n pháº©m
                </h5>
            </div>
            <div class="card-body">
                @await Html.PartialAsync("_Alerts")
                @if (Model.Products != null && Model.Products.Any())
                {
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th>ID</th>
                                    <th>TÃªn sáº£n pháº©m</th>
                                    <th>GiÃ¡</th>
                                    <th>Sá»‘ lÆ°á»£ng</th>
                                    <th>Danh má»¥c</th>
                                    <th>Tráº¡ng thÃ¡i</th>
                                    <th>Thao tÃ¡c</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var product in Model.Products)
                                {
                                    <tr>
                                        <td>@product.Id</td>
                                        <td>@product.ProductName</td>
                                        <td>@product.Price.ToString("C0")</td>
                                        <td>@product.StockQuantity</td>
                                        <td>@product.CategoryName</td>
                                        <td>
                                            <span class="badge bg-success">CÃ²n hÃ ng</span>
                                        </td>
                                        <td>
                                            <div class="btn-group" role="group">
                                                <a href="/Staff/Products/Edit/@product.Id" class="btn btn-sm btn-outline-primary">
                                                    <i class="fas fa-edit"></i> Sá»­a
                                                </a>
                                                <a href="/Staff/Products/Delete/@product.Id" class="btn btn-sm btn-outline-danger">
                                                    <i class="fas fa-trash"></i> XÃ³a
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <nav aria-label="Product pagination" class="mt-3">
                        <form method="get" class="d-flex gap-2 mb-2">
                            <input class="form-control" type="text" name="search" value="@Model.Search" placeholder="Tìm sản phẩm..." />
                            <input type="hidden" name="size" value="@Model.Size" />
                            <button class="btn btn-outline-primary" type="submit">Tìm</button>
                        </form>
                        <ul class="pagination">
                            <li class="page-item @(Model.Page <= 1 ? "disabled" : null)">
                                <a class="page-link" href="?page=@(Model.Page-1)&size=@Model.Size&search=@Model.Search">Previous</a>
                            </li>
                            @for (int i = 1; i <= Model.TotalPages; i++)
                            {
                                <li class="page-item @(i == Model.Page ? "active" : null)">
                                    <a class="page-link" href="?page=@i&size=@Model.Size&search=@Model.Search">@i</a>
                                </li>
                            }
                            <li class="page-item @(Model.Page >= Model.TotalPages ? "disabled" : null)">
                                <a class="page-link" href="?page=@(Model.Page+1)&size=@Model.Size&search=@Model.Search">Next</a>
                            </li>
                        </ul>
                    </nav>
                }
                else
                {
                    <div class="text-center py-5">
                        <i class="fas fa-box fa-3x text-muted mb-3"></i>
                        <h5 class="text-muted">ChÆ°a cÃ³ sáº£n pháº©m nÃ o</h5>
                        <p class="text-muted">HÃ£y thÃªm sáº£n pháº©m Ä‘áº§u tiÃªn cá»§a báº¡n!</p>
                        <a href="/Staff/Products/Create" class="btn btn-primary">
                            <i class="fas fa-plus"></i> ThÃªm sáº£n pháº©m
                        </a>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

